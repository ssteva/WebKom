using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Linq;
using System.Reflection;
using System.Web;
// using Microsoft.AspNet.SignalR;
// using Microsoft.AspNet.SignalR.Hubs;

namespace webkom.Hubs
{
    // public class AppHub : Hub
    // {
    //     //private static readonly log4net.ILog Log = log4net.LogManager.GetLogger(MethodBase.GetCurrentMethod().DeclaringType);
    //     private static readonly ConcurrentDictionary<string, List<Guid>> Mapping = new ConcurrentDictionary<string, List<Guid>>();
    //     public void Hello()
    //     {
    //         //Log.Info("Pozvan je hello " + DateTime.Now);
    //         Clients.All.info("Ping " + DateTime.Now);
    //     }
    //     public void Test() //dummy metod
    //     {
    //         //Log.Info("Pozvan je test " + DateTime.Now);
    //         Clients.All.test("Ping test " + DateTime.Now);
    //     }
    // }
}